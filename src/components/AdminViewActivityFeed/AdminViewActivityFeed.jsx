import React, { useState, useEffect } from 'react';
import { useSelector, useDispatch } from 'react-redux';
import ActivityCardContent from '../ActivityCardContent/ActivityCardContent';
// material ui imports
import { Box, Card, Grid, IconButton, FormControlLabel, FormGroup, 
    List, ListItem, ListItemText, Switch, Typography } from '@mui/material';
import EditIcon from '@mui/icons-material/Edit';

function AdminViewActivityFeed() {

    return (
        <>
        </>
    )
}

export default AdminViewActivityFeed;